import {Heading, Subheading, HeaderContainer} from './loginheader.styled';

const FormHeader = () => {
	return (
		<HeaderContainer>
			<Heading>Let's Get Started</Heading>
			<Subheading>Continue with your favourite social media</Subheading>
		</HeaderContainer>
	);
};

export default FormHeader;
